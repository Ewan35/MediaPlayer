{% extends 'layout.html.twig' %}

{% block main %}

    <h2>Add medias</h2>

    {{ form_start(mediaForm, {'attr': {'novalidate': 'novalidate'}} ) }}
    {{ form_widget(mediaForm) }}
    <button type="submit" class="btnValider orange">Validate</button>
    {{ form_end(mediaForm) }}

{% endblock %}

