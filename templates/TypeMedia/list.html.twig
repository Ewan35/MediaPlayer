{% extends 'layout.html.twig' %}

{% block main %}
    <h1>Types de média</h1>
    <div class="media-list">
        {% for typeMedia in typeMedias %}
            <div>
                {{ typeMedia.name }}
                <a href="{{ path('TypeMedia_update', {'id' : typeMedia.id }) }}" class="oi oi-delete update-media" data-glyph="pencil" title="Update media" data-id="{{ typeMedia.id }}" >Modifier</a>
                <button class="oi oi-delete delete-media delete-modal" data-glyph="circle-x" title="Delete type media" data-id="{{ typeMedia.id }}" >Supprimer</button>
            </div>
        {% endfor %}
    </div>

    <button><a href="{{ path('TypeMedia_add') }}">Ajouter</a></button>

{% endblock %}
